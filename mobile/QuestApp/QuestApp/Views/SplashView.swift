struct SplashView: View {
    @EnvironmentObject var router: Router
    
    var body: some View {
        VStack {
            // Mevcut SplashView içeriği
        }
        .accessibility(identifier: "splashView")
    }
} 